# Tests adding two nodes to the graph

# Create a graph
CreateGraph twoNodesGraph

# Add two nodes
AddNode twoNodesGraph a
AddNode twoNodesGraph b

# Print the nodes in the graph, the outgoing edges from a and the outgoing edges from b
ListNodes twoNodesGraph
ListChildren twoNodesGraph a
ListChildren twoNodesGraph b